// This is the traditional way
const user = { userName: "Aureliano Buendía", age: 32, city: "Macondo" };
const userName = user.userName;
const age = user.age;
const city = user.city;

console.log(`${userName}, ${age}, ${city}`); // Aureliano Buendía, 32, Macondo
