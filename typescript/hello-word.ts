let helloWorld: string = "Hello, World!";

console.log(helloWorld);  // Hello, World!

// Interface declaration

interface Client {
    name: string,
    id: 0
}

const client: Client = {
    name: "Alice",
    id: 0
};

console.log(client);  // { name: 'Alice', id: 0 }
